import { z } from 'zod';
import { type QueryResultRow, type SqlTaggedTemplate } from '../types';
export declare const createSqlTag: <K extends keyof Z, P extends z.ZodTypeAny, Z extends Record<K, P>, T extends QueryResultRow = QueryResultRow>(configuration?: {
    typeAliases?: Z | undefined;
}) => SqlTaggedTemplate<Z, T>;
