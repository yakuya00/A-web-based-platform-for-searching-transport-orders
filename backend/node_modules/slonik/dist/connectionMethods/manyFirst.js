import { query } from './query.js';
import { generateUid } from '@slonik/utilities';
export const manyFirst = async (log, connection, clientConfigurationType, slonikQuery, inheritedQueryId) => {
    const queryId = inheritedQueryId ?? generateUid();
    const { rows } = await query(log, connection, clientConfigurationType, slonikQuery, queryId, {
        validationType: 'MANY_ROWS_ONE_COLUMN',
    });
    const keys = Object.keys(rows[0]);
    const firstColumnName = keys[0];
    return rows.map((row) => {
        return row[firstColumnName];
    });
};
//# sourceMappingURL=manyFirst.js.map