import { stringify } from 'node:querystring';
export const stringifyDsn = (connectionOptions) => {
    const { applicationName, databaseName, host, options, password, port, sslMode, username, } = connectionOptions;
    const tokens = ['postgresql://'];
    if (username) {
        tokens.push(encodeURIComponent(username));
        if (password) {
            tokens.push(':', encodeURIComponent(password));
        }
        tokens.push('@');
    }
    tokens.push(host ?? '');
    if (port) {
        tokens.push(':', String(port));
    }
    if (databaseName) {
        tokens.push('/', encodeURIComponent(databaseName));
    }
    const namedParameters = {};
    if (applicationName) {
        // eslint-disable-next-line canonical/id-match
        namedParameters.application_name = applicationName;
    }
    if (options) {
        namedParameters.options = options;
    }
    if (sslMode) {
        namedParameters.sslmode = sslMode;
    }
    if (Object.keys(namedParameters).length > 0) {
        tokens.push('?', stringify(namedParameters));
    }
    return tokens.join('');
};
//# sourceMappingURL=stringifyDsn.js.map